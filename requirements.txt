# SmartInbox Backend – Python dependencies
# Install with:  pip install -r requirements.txt

# ── FastAPI core ─────────────────────────────────────────────────────────────
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.9          # form-data / file upload support
aiofiles>=23.2.0                 # async file I/O + streaming CSV

# ── Rate limiting ─────────────────────────────────────────────────────────────
slowapi>=0.1.9                   # rate limiting for FastAPI

# ── Database ─────────────────────────────────────────────────────────────────
sqlalchemy[asyncio]>=2.0.0
asyncpg>=0.29.0                  # async PostgreSQL driver
alembic>=1.13.0                  # database migrations

# ── Authentication ────────────────────────────────────────────────────────────
python-jose[cryptography]>=3.3.0 # JWT (RS256 / HS256)
passlib[bcrypt]>=1.7.4           # password hashing
bcrypt>=4.0.0                    # used directly in auth/password.py
python-dotenv>=1.0.0             # .env file loading fallback

# ── Validation / Settings ────────────────────────────────────────────────────
pydantic>=2.7.0
pydantic-settings>=2.3.0
email-validator>=2.1.0           # EmailStr support
requests>=2.31.0
aiohttp>=3.9.0                   # async HTTP for Groq API calls

# ── ML core ──────────────────────────────────────────────────────────────────
scikit-learn>=1.4.0
numpy>=1.26.0
pandas>=2.1.0
scipy>=1.12.0
nltk>=3.8.0
imbalanced-learn>=0.12.0
matplotlib>=3.8.0
seaborn>=0.13.0
joblib>=1.3.0                   # parallel ensemble persistence

# ── ML Ensemble (boosting classifiers) ───────────────────────────────────────
xgboost>=2.0.0
lightgbm>=4.0.0

# ── Groq LLM API (Layer 2 Semantic Analysis) ─────────────────────────────────
groq>=0.5.0                      # Official Groq Python client

# ── Optional: Semantic embeddings (uncomment for GPU deployments) ─────────────
# sentence-transformers>=2.7.0  # ~90MB; requires torch; adds 50-200ms/msg latency
# torch>=2.0.0

