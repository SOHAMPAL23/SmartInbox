{
  "name": "smartinbox-root",
  "version": "1.0.0",
  "description": "Root configuration to boot both Frontend and Backend concurrently.",
  "scripts": {
    "build": "npm --prefix Frontend run build",
    "dev": "start cmd /k \"npm run dev:backend\" && start cmd /k \"npm run dev:frontend\"",
    "dev:backend": "cd .venv\\Scripts && activate.bat && cd ..\\.. && python -m uvicorn app.main:app --reload --host 0.0.0.0 --port 8000",
    "dev:frontend": "cd frontend && npm run dev"
  }
}
